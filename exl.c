#include<stdio.h>
int main(int argc, char const *argv[])
{
   int a;
    printf("Enter the five digit value\n");
    scanf("%d",&a);
    printf("We have to finx the no formed after adding one to each digit\n");
    printf("The no required is %d", a+11111);
    return 0;
}
